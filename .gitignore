# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
.env

# Node
node_modules/
.next/
out/
.env.local
*.tsbuildinfo

# TypeScript incremental build cache — machine-local, must NOT be committed
# (a merged/stale copy desyncs incremental builds after conflict fixes).
*.tsbuildinfo

# Editor
.DS_Store
*.swp
*.swo

# Docker volumes
postgres-data/

# Nested clone of separate repo (cognixoneai/cognix) - not tracked here
docs/cognix/
# Large local artifacts
L2_implementation_June5.zip
L2_implementation_June5/
